#include <stdio.h>
#include <stdlib.h>
#include <malloc.h>

#include "./Headers/arr.h"
 
int main() {
    choiceArr();
    int* arrF;
    int vol;
    if ( arrType == 1){
        enterArr ();
    }

    choiceMethod ();
    /*
    printArr();
    for (int i = 0; i < arrCout; i++){
        vol = size[i];
        arrF = arr[i];
        
        //bubbleFunc(mass, N);
        //shakeFunc(mass, N);
        //insertionFunc(mass, N);
        //combFunc(mass, N);
        selectionFunc (arrF, vol);
    }

    printArr();
 
    free(arrF);*/
    return 0;
}